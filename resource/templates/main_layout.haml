<!DOCTYPE html>
%html lang="en"
  %head
    %meta http-equiv="content-type" content="text/html; charset="utf-8"
    -if title
      %title 
        = title + " - Resource"
    -else
      %title
        Resource
    %link rel=stylesheet type=text/css href="{{ url_for('static', filename='css/bootstrap.css') }}"
    %link rel=stylesheet/less type=text/css href="{{ url_for('static', filename='css/style.less') }}"
    %style
      body {
       padding-top: 60px;
       padding-bottom: 40px;
      }
    %script src="{{ url_for('static', filename='js/lib/less.min.js') }}" type="text/javascript"

  %body.claro
    %div.navbar.navbar-fixed-top
      %div.navbar-inner
        %div.container
          %a.brand href="/"
            Resource
          %div.nav-collapse.collapse
            %ul class="nav"
              %li
                %a href="repository"
                  Repository
              %li
                %a href="dlab_bookings"
                  Dlab bookings
              %li
                %a href="hello"
                  Hello
            -if session['user_uid'] == None
              %form class="navbar-form pull-right" action="/login" method="post"
                %input class="span2" type="text" placeholder="Username" name="username"
                %input class="span2" type="password" placeholder="Password" name="password"
                %button class="btn" type="submit"
                  Sign in
            -else
              %p.pull-right.navbar-text.nav
                =session['user_uid']
                %a href="/logout"
                  Logout

    %div.container.content
      %div.row
      -with messages = get_flashed_messages()
        -if messages
          %ul
            -for message in messages
              %li
                {{ message }}
      -block body
      %hr.
      %footer
        %p.pull-right
          %a href="http://flask.pocoo.org/"
            %img src="http://flask.pocoo.org/static/badges/made-with-flask-s.png" border="0" alt="made with Flask"
          by Mandla Mbuli
    %script src="{{ url_for('static', filename='js/lib/dojo/dojo.js') }}" type="text/javascript" data-dojo-config="async: true, parseOnLoad: 1"
    %script src="{{ url_for('static', filename='js/main.js') }}" type="text/javascript"